version: '3.8'

services:
  redis:
    image: redis:7.2-alpine
    container_name: local_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data: